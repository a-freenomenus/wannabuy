#left-column
  = render :partial => 'users/profile_navigation'

#right-column
  %h1
    Редактирование профиля

  = form_for(@user) do |f|
    = render 'form', :f => f
    .buttons
      = f.submit "Update"
