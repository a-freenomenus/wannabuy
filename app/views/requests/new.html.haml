= content_for :breadcrumbs do
  = breadcrumbs ['Главная', root_path], ['Мои запросы', profile_requests_path], ['Новый запрос']

#full-width.border
  #request_new_form
    = form_for(@request) do |f|
      = render 'form', :f => f
      
      %ul.buttons
        %li
          =f.submit "Сохранить"
